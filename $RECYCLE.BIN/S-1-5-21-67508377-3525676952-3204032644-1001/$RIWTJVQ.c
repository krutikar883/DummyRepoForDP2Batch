#include<stdio.h>
void main()
{
	int a,b,mul;
	printf("enter a,b values");
	scanf("%d%d",&a,&b);
	a=mul*10;
	b=mul;
    mul=a*b;
	printf("enter multiplication of firstDigit and lastDigit is %d",mul);
	getch();
	
}

